# Tamga Protocol — çekirdek bağımlılıkları (sıfır-bağımlılık ilkesine istisna yalnız bu ikisi)
# RFC gerekçesi: PyNaCl =libsodium bağlayıcısı (ed25519/XChaCha20); jsonschema = RFC-001 şema-doğrulama.
PyNaCl>=1.5,<2
jsonschema>=4.20,<5
